#[derive(Clone)]
#[allow(dead_code)]
pub(super) enum TensorOperation {
    Constant,
    Add,
    Sub,
    Div,
    ScalMul,
    ElemMul,
    MatMul,
    Transpose,
    Abs,
    Ln,
    Sqrt,
    Neg,
    Exp,
    Pow,
    PowF,
    Mean,
    Sum,
    Max,
    Map,
    Sigmoid,
    Relu,
    Tanh,
}
